const styles = (theme) => {
  return {
    FilterInput: {
      maxWidth: '65vw',
      minWidth: '40vw',
    },
    ChosenItemChips: {
      display: 'flex',
      marginBottom: 2,
    },
  };
};

export default styles;

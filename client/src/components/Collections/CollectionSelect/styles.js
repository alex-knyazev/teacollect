const styles = (theme) => {
  return {
    CollectionsList: {
      margin: '4px 0 4px 0',
      display: 'flex',
    },
    CollectionButton: {
      textTransform: 'capitalize',
    },
    TabButton: {
      textTransform: 'capitalize',
      borderRadius: 6,
    },
    labelContainer: {
      fontSize: 15,
      padding: 0,
    },
  };
};
